## Zombie Manager Node API

### Setup Steps

In order to run the backend server you need to:

* Use one terminal to run: `mongod` --dbpath ~/data/db // (or the path to your `"data/db"` location)
* Use another one to run `yarn`
* And then `yarn start`

And that's it!
